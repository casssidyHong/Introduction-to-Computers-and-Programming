#include <stdio.h>
#include <string.h>

struct f
{
    char 
    f * next;
};

head = new f;
f * current = head;

current -> next = f;
current = current -> next;

int main()
{
    int n;
    scanf("%d\n", &n);
    for(int i=0; i<n; i++)
    {
        scnaf("")
    }
    printf("Hello World");

    return 0;
}
