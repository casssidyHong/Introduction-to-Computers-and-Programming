#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main()
{
    int n;
    int A[35], B[35], C[35];
    scanf("%d\n", &n);
    for(int i=0; i<n; i++)
    {
        scanf("%d %d %s\n", &A[i], &B[i], &Q[i]);
    }
    scanf("%d\n", &y);
    return 0;
}
